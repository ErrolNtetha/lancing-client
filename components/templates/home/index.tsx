import React from 'react';
import { ClientProject } from '../../organisms/project';

export const Home = () => {
    return (
        <section className=''>
            Client component
            <ClientProject />
        </section>
    );
};
